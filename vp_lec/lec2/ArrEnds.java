import java.util.Arrays;
/** A class to think about dealing with array ends. It occurred to me
 * that doing this with Arrays, and dealing with the trade-offs by 
 * not using ArrayLists, may be useful in many cases. I just don't
 * have familiarity with copying, adding, removing, resizing, etc, 
 * creating a subarray, in java, as in python.
 * 
 * Also noting that ArrayLists exist and have the flexibility. */
public class ArrEnds {

	public static void main(String[] args) {
		int[] z0 = new int[]{0,1,2,3,4,5,6,7};
		System.out.println(Arrays.toString(z0));

		z0
	}
}